class Turtle {
  constructor(id, name, speed, age, weightKg) {
    this.id = id; //number
    this.name = name; //string
    this.speed = speed; //string
    this.age = age; //number
    this.weightKg = weightKg; //number
  }
}

module.exports = Turtle;
